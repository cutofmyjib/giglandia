<div data-role="content" class="content">
  <h1>Find a band</small></h1>
    <form method="post" action="/bands">
      <input type="search" name="search_band" id="search-mini" value="" data-mini="true" />
    </form>

    <% if @bands %>
      <% @bands.to_hash.map do |band| %>
        <p><%= band.display_name %></p>
      <% end %>
    <% end %>
</div>